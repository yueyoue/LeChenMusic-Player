# LeChenMusic 影视模块开发总结

> 最后更新: 2026-07-21 (v3 - 新增分类缓存+分页+筛选)
> 维护者: AI Assistant + 项目负责人

---

## 项目概述

**LeChenMusic** 是一个音乐+有声书+影视的 Android APP(Jetpack Compose),服务端基于 Navidrome/Subsonic 协议,影视功能对接 LunaTV。

- **APP端仓库**: https://github.com/yueyoue/LeChenMusic-Player
- **服务端仓库**: https://github.com/yueyoue/LeChenMusic-Server
- **LunaTV 服务**: http://j.tthsdd.top:3000 (admin / shaozi1981)
- **LunaTV 源码**: https://github.com/MoonTechLab/LunaTV
- **参考APP(Selene-Source)**: https://github.com/MoonTechLab/Selene-Source (Flutter,为 LunaTV 配套)

**核心原则**: 所有影视功能直接参考 Selene-Source 的实现方法。遇到问题第一时间看 Selene-Source 的解决方案。

---

## 一、LunaTV 实际 API(已验证)

⚠️ **LunaTV 返回的是裸 JSON,没有 code/msg/data 包装!**

| 接口 | 方法 | 实际返回格式 | 注意事项 |
|------|------|-------------|---------|
| `/api/login` | POST | `{"ok": true}` | body: `{"username":"admin","password":"***"}` |
| `/api/search` | GET | `{"results": [VideoInfo...]}` | **参数用 `q` 不是 `keyword`** |
| `/api/detail` | GET | `VideoDetail` 裸对象 | 参数: `source`, `id` |
| `/api/playrecords` | GET | `{"source+id": {record...}}` | **是 dict 不是 list** |
| `/api/favorites` | GET | `[VideoInfo...]` | 裸数组 |
| `/api/search/resources` | GET | `[SearchResource...]` | 裸数组 |
| `/api/live/sources` | GET | `[LiveSource...]` | - |
| `/api/live/channels` | GET | `[LiveChannelGroup...]` | - |

**不存在的接口**(返回 404): `/api/home`, `/api/category`

### 搜索结果 VideoInfo 关键字段
```json
{
  "id": "65490", "title": "痴迷2025", "poster": "https://...",
  "episodes": ["https://xxx.m3u8", ...],
  "episodes_titles": ["1", "2", ...],
  "source": "iqiyizyapi.com", "source_name": "🎬-爱奇艺-",
  "year": "2025", "type_name": "movie", "douban_id": 0
}
```

### 详情 VideoDetail 关键字段
```json
{
  "id": "80631", "title": "", "poster": "https://...",
  "episodes": ["https://xxx.m3u8"], "episodes_titles": ["1"],
  "source": "iqiyizyapi.com", "source_name": "🎬-爱奇艺-",
  "year": "2025", "desc": "", "type_name": "", "douban_id": 0
}
```

### 播放记录格式(dict,key 是 "source+id")
```json
{
  "apilsbzy1.com+313262": {
    "title": "xxx", "cover": "https://...", "year": "2026",
    "source_name": "...", "index": 1, "total_episodes": 1,
    "play_time": 474, "total_time": 1465, "save_time": 1783320732423
  }
}
```

---

## 二、已修复的问题(共 42 项)

### 第一批: API 适配(7项)

| # | 问题 | 修复 |
|---|------|------|
| 1 | 搜索参数用了 `keyword`,实际是 `q` | `VideoApiClient.kt` 改 `@Query("q")` |
| 2 | `DoubanApiClient` JVM 签名冲突 | 属性改名 `_api` |
| 3 | 豆瓣 API 不再返回 `cover_url` | 新增 `DoubanPic` 模型,读 `pic.normal` |
| 4 | 豆瓣图片 418 被拦截 | CDN 替换为 `img3.doubanio.com` |
| 5 | `/api/category` 不存在 | 改用搜索接口实现分类 |
| 6 | 代码期望 `code/msg/data` 包装 | 重写数据模型,移除包装层 |
| 7 | 播放记录格式(list→dict) | 从 dict 解析,key 提取 source+id |

### 第二批: 闪退修复(4项)

| # | 问题 | 根因 | 修复 |
|---|------|------|------|
| 8 | 点击热门电影闪退 | searchAndPlay 无加载提示,episodes 为空直接崩 | 添加搜索弹窗+空数据保护 |
| 9 | 最近观看点播放闪退 | 某些源 detail 返回空 episodes | loadDetail 自动换源+空数据保护 |
| 10 | **ExoPlayer 闪退(核心根因)** | **缺少 `media3-exoplayer-hls` 依赖** | build.gradle 添加 HLS 依赖 |
| 11 | ErrorReporter token 为空 | init 时传空字符串 | 改为动态读取 `NavidromeAuth.token` |

### 第三批: 播放器+详情页重构(4项)

| # | 问题 | 修复 |
|---|------|------|
| 12 | 播放器黑屏(只有音频) | 用 `AndroidView`+`PlayerView` 替代纯 Compose |
| 13 | 强制全屏无法退出 | 自定义控件+BackHandler+用户主动全屏 |
| 14 | PlayerView 设置按钮遮挡全屏按钮 | 禁用内置控制器,自定义叠加层 |
| 15 | searchAndPlay 直接跳全屏播放 | 改为跳详情页(内联播放器+影片信息) |

### 第四批: 搜索弹窗+片源+播放器交互(3项)

| # | 问题 | 根因 | 修复 |
|---|------|------|------|
| 16 | 点击影片不弹搜索弹窗 | `finally` 块在导航前就清除了 `searchSourceLoading` | 移到 `consumeNavigateToDetail` 中清除 |
| 17 | 片源切换不显示 | `searchAndPlay` 只取了一个源 | 保存所有有效源到 `allSearchSources` |
| 18 | 全屏切换闪回+进度条不可拖动 | `LaunchedEffect` 重组时重复触发 | 改为 `DisposableEffect`;进度条改为 `Slider` |

### 第五批: 导航失效修复(2项)

| # | 问题 | 根因 | 修复 |
|---|------|------|------|
| 19 | **点击推荐电影无反应** | **`LaunchedEffect` 放在 LazyColumn `item{}` 内部,滚动时被回收** | 移到 Scaffold 层级(LazyColumn 外面) |
| 20 | 日志显示字面量 `${results.size}` | `${'$'}` 转义导致字符串不插值 | 改为正确的 Kotlin 字符串插值 |

### 第六批: 片源+全屏+进度条(4项)

| # | 问题 | 根因 | 修复 |
|---|------|------|------|
| 21 | 片源显示580个,切换后无选集 | 全量显示;索引不匹配 | 去重+限制20个;选集改用 `detail.episodes` |
| 22 | 全屏播放闪回小屏 | AndroidManifest 没有 `configChanges` | 添加 `configChanges` 防止 Activity 重建 |
| 23 | 进度条拖动卡顿 | 200ms 轮询导致频繁重组 | 改为 500ms 轮询 |
| 24 | switchSource 日志源名为空 | 用 `sourceName`(空字段) | 改为 `displaySourceName` |

### 第七批: 源切换核心修复(5项) ⭐

| # | 问题 | 根因 | 修复 |
|---|------|------|------|
| 25 | 片源切换播放器停止 | LaunchedEffect 读 `currentDetail` 但不是 key | 改用 `switchVersion` 计数器 |
| 26 | 播放时息屏 | 缺少 `FLAG_KEEP_SCREEN_ON` | 播放时添加屏幕常亮标志 |
| 27 | 手势拖不动 | `clickable` 吞掉后续手势事件 | `pointerInput` 移到 `clickable` 前面 |
| 28 | 片源切换日志源名为空 | 用 `sourceInfo.sourceName` | 改为 `sourceInfo.displaySourceName` |
| 29 | 分类导航直接进空白详情页 | `source.isNotBlank()` 为 true 走 `loadDetail` | 统一走 `searchAndPlay` |

### 第八批: 源切换终极修复(3项) ⭐⭐

| # | 问题 | 根因 | 修复 |
|---|------|------|------|
| 30 | 片源切换仍不播放 | 两个 LaunchedEffect 互相干扰 | 合并为一个;直接在 onClick 中控制 ExoPlayer |
| 31 | 位置记忆不生效 | `seekTo` 在 `prepare()` 后立即调用,播放器未 ready | 用 `Player.Listener` 等 `STATE_READY` 后再 seek |
| 32 | 进度条拖动卡顿 | `Slider` 组件框架开销大 | 替换为自定义 `pointerInput` 手势进度条(参考音乐播放器) |

### 第九批: 源测速+源列表面板(3项)

| # | 问题 | 修复 |
|---|------|------|
| 33 | 源切换位置记忆 | `seekTo` 改用 `Player.Listener` 等 `STATE_READY` |
| 34 | 源测速 | `testSourceSpeeds()` OkHttp HEAD 请求测 ping |
| 35 | 源列表面板 | ModalBottomSheet 显示全部源+速度+展开按钮 |

### 第十批: 搜索匹配+播放记录(3项)

| # | 问题 | 根因 | 修复 |
|---|------|------|------|
| 36 | 搜索匹配错误("晚餐"→"最后的猪肉晚餐") | `contains` 匹配太宽松 | 改为精确匹配(参考 Selene-Source `titleMatch`) |
| 37 | 分类页顶部空白 | TopAppBar 自动添加 status bar padding | `windowInsets = WindowInsets(0,0,0,0)` |
| 38 | 播放记录不保存 | 没有自动保存机制 | 每10秒自动保存播放进度(参考 Selene-Source) |

---

## 三、当前状态总结(2026-07-21)

| 模块 | 状态 | 说明 |
|------|------|------|
| 首页推荐(豆瓣) | ✅ 正常 | 热门电影/剧集/动漫/综艺 |
| 搜索 LunaTV | ✅ 正常 | 精确匹配标题 |
| 搜索弹窗 | ✅ 正常 | 点击影片后显示"正在搜索播放源" |
| ExoPlayer 播放 | ✅ 正常 | HLS m3u8 可播放 |
| 内联播放器 | ✅ 正常 | 自定义手势进度条+时间显示 |
| 详情页信息 | ✅ 正常 | 封面/标题/评分/导演/演员 |
| 片源切换 | ✅ 正常 | 直接控制 ExoPlayer + 位置记忆 |
| 源测速 | ✅ 正常 | ping 测速+排序 |
| 源列表面板 | ✅ 正常 | 展开按钮+全部源+速度显示 |
| 全屏切换 | ✅ 正常 | configChanges 防止重建 |
| 恒屏 | ✅ 正常 | FLAG_KEEP_SCREEN_ON |
| 播放记录保存 | ✅ 正常 | 每10秒自动保存 |
| 分类页 | ✅ 正常 | 搜索 LunaTV 显示结果 |
| 直播 | ✅ 基本可用 | - |
| 收藏 | ✅ 正常 | - |
| 分类页筛选功能 | ✅ 已实现 | 年份/地区/来源三维筛选+筛选面板+标签 |
| 分类页缓存 | ✅ 已实现 | 10分钟缓存+滚动加载更多(客户端分页) |
| 首页热门短剧 | ✅ 已实现 | LunaTV 搜索"短剧"+豆瓣热门综艺双数据源 |
| 播放器手势控制 | ✅ 已实现 | 左侧亮度/右侧音量/水平快进(ExoPlayer 直接控制) |
| 投屏功能(DLNA) | 🟢 未实现 | 需要集成 DLNA 库 |
| 响应式布局 | 🟢 未实现 | 手机/平板/车机适配 |

---

## 四、待开发任务清单

### ~~🟡 P1: 分类页筛选功能~~ ✅ 已完成

**实现方案**: 客户端筛选(LunaTV 搜索结果已包含 year/area/source 字段)
- **年份**: 全部/2026/2025/2024/2023/更早
- **地区**: 从搜索结果中自动提取(爱奇艺→中国大陆, Netflix→欧美 等)
- **来源**: 从搜索结果中自动提取(displaySourceName)
- **UI**: 顶部筛选按钮 → 展开筛选面板(横向滚动 Chip) + 已选标签行
- **交互**: 点击 Chip 切换筛选 → 立即过滤 → 显示结果数

**修改文件**: `VideoViewModel.kt`(CategoryFilters+筛选逻辑), `VideoCategoryScreen.kt`(筛选面板UI)

### ~~🟡 P1: 分类页缓存+分页~~ ✅ 已完成

**实现方案**:
- **缓存**: ViewModel 中 `categoryCache` Map, key=keyword, value=(timestamp, fullList), TTL=10分钟
- **分页**: 客户端分页, PAGE_SIZE=20, 滚动到底部自动加载更多
- **去重**: 按 source+id 去重搜索结果
- **两处入口**: VideoScreen(内联Tab) + VideoCategoryScreen(独立页) 都支持分页

**修改文件**: `VideoViewModel.kt`(缓存+分页逻辑), `VideoCategoryScreen.kt`(分页UI), `VideoScreen.kt`(分页UI)

### ~~🟡 P1: 首页热门短剧~~ ✅ 已完成

**实现方案**: 
- 从 LunaTV 搜索"短剧"关键词获取,去重取前20个
- 豆瓣热门综艺作为第二数据源(fallback)
- 合并去重后显示在首页"热门短剧"分区
- 搜索过程静默处理,失败不影响其他分区

**修改文件**: `HomeScreen.kt`, `VideoViewModel.kt`

### 🟢 P2: 投屏功能(DLNA)

**参考**: Selene-Source `lib/widgets/dlna_player.dart` + `dlna_dart` 库
**Android 方案**: Cling 库或 android-upnp-control
**实现**: 搜索局域网 DLNA 设备 → 推送视频 URL → 控制播放

### ~~🟢 P2: 播放器手势控制~~ ✅ 已完成

**实现方案**:
- **左侧上下滑动**: 调节屏幕亮度(WindowManager.LayoutParams.screenBrightness)
- **右侧上下滑动**: 调节系统音量(AudioManager)
- **水平滑动**: 快进/快退(ExoPlayer 直接 seekTo,增量±10秒)
- **视觉反馈**: 滑动时显示半透明 HUD(亮度🔆/音量🔊/进度⏩图标+数值)
- **防冲突**: 手势区域严格三分(左30%/中40%/右30%),避免与进度条拖动冲突
- **参考音乐播放器**: 使用 `detectDragGestures` + `pointerInput`,与内联播放器进度条手势不冲突

**修改文件**: `VideoPlayerScreen.kt`

**关键实现细节**:
```kotlin
// 三区域手势检测
if (x < width * 0.3f) → 左侧: 累加 deltaY → 亮度
else if (x > width * 0.7f) → 右侧: 累加 deltaY → 音量  
else → 中间: 累加 deltaX → 快进快退(seekTo)

// ExoPlayer 直接控制(不依赖中间状态)
val seekTarget = (exoPlayer.currentPosition + deltaMs).coerceIn(0, duration)
exoPlayer.seekTo(seekTarget)
```

### 🟢 P2: 响应式布局

**适配**: 手机(单列)/平板(网格+侧边栏)/车机(大字体+简化导航)

---

## 五、项目架构

### 文件结构
```
app/src/main/java/com/lechenmusic/
├── data/
│   ├── api/
│   │   ├── ApiClient.kt          # 音乐 API + NavidromeAuth
│   │   ├── SubsonicApi.kt        # 音乐接口定义
│   │   └── VideoApiClient.kt     # 影视 API(LunaTV + 豆瓣) ⭐
│   ├── model/
│   │   ├── SubsonicModels.kt     # 音乐数据模型
│   │   └── VideoModels.kt        # 影视数据模型 ⭐
│   └── repository/
│       └── SettingsRepository.kt # 设置存储(DataStore)
├── ui/
│   ├── MainViewModel.kt          # 音乐主 ViewModel
│   ├── VideoViewModel.kt         # 影视 ViewModel ⭐
│   ├── navi/Screen.kt            # 路由定义
│   ├── screens/
│   │   ├── home/HomeScreen.kt    # 首页(三模式切换) ⭐
│   │   └── video/
│   │       ├── VideoScreen.kt         # categoryName 工具函数
│   │       ├── VideoSearchScreen.kt   # 搜索页
│   │       ├── VideoDetailScreen.kt   # 详情页(内联播放器) ⭐⭐
│   │       ├── VideoPlayerScreen.kt   # 全屏播放器
│   │       ├── VideoCategoryScreen.kt # 分类页
│   │       └── LiveScreen.kt         # 直播
│   └── components/
├── player/                       # 音乐播放器
├── LeChenApp.kt                  # Application(Coil+ErrorReporter)
└── MainActivity.kt               # 路由注册 ⭐
```

### 关键配置
- **compileSdk**: 34, **minSdk**: 26, **targetSdk**: 34
- **签名**: `release.keystore.p12`, alias: `lechenmusic`, pass: `lechen123`
- **Media3**: `media3-exoplayer:1.2.1` + `media3-exoplayer-hls:1.2.1` + `media3-ui:1.2.1`

### 影视模块数据流
```
首页(豆瓣推荐) → 点击影片 → searchAndPlay(title)
  → 搜索 LunaTV(精确匹配) → 构造 VideoDetail → 导航到详情页
  → VideoDetailScreen: 内联播放器 + 片源列表 + 选集
  → 片源切换: 直接控制 ExoPlayer(stop → setMediaItem → prepare → seekTo)
  → 播放记录: 每10秒自动保存到 LunaTV
```

---

## 六、参考项目 Selene-Source 关键实现

### 1. 搜索匹配规则(核心!)
```dart
// lib/screens/player_screen.dart:2547
final titleMatch = result.title.replaceAll(' ', '').toLowerCase() ==
    (widget.title.replaceAll(' ', '').toLowerCase());
final yearMatch = widget.year == null ||
    result.year.toLowerCase() == widget.year!.toLowerCase();
// 类型匹配: tv→episodes>1, movie→episodes==1
return titleMatch && yearMatch && typeMatch;
```
**我们已采用**: 精确标题匹配 → contains 模糊匹配 → 取第一个

### 2. 源切换方案(核心!)
```dart
// lib/screens/player_screen.dart:949 _switchSource()
void _switchSource(SearchResult newSource) async {
  final currentProgress = currentPosition?.inSeconds ?? 0;  // 保存位置
  currentDetail = newSource;                                  // 更新数据
  startPlay(currentEpisode, currentProgress);                 // 直接播放
}
```
**我们已采用**: onClick 中直接 `exoPlayer.stop() + setMediaItem + prepare + seekTo`

### 3. 播放记录保存
```dart
static const Duration _saveProgressInterval = Duration(seconds: 10);
// 定时保存 + 返回按钮 + 下一集 + 应用后台
```
**我们已采用**: LaunchedEffect 每10秒保存

### 4. 图片处理
```dart
originalUrl.replaceAll(RegExp(r'img\d+\.doubanio\.com'), 'img3.doubanio.com')
// + Referer + User-Agent 请求头
```

### 5. 关键文件
- `lib/screens/player_screen.dart` - 播放器(3354行,搜索+播放一体)
- `lib/screens/movie_screen.dart` - 分类页(筛选+分页)
- `lib/widgets/player_sources_panel.dart` - 片源面板(测速+排序)
- `lib/services/m3u8_service.dart` - M3U8 测速服务
- `lib/utils/image_url.dart` - 图片 URL 处理

---

## 七、给下位程序员的关键提示 ⚠️

### 1. 绝对不要踩的坑

| 坑 | 说明 | 后果 |
|----|------|------|
| **ExoPlayer HLS 依赖** | 必须有 `media3-exoplayer-hls` | 没有它播放 m3u8 直接 native 崩溃,try-catch 捕获不到 |
| **LunaTV 搜索参数** | 用 `q` 不是 `keyword` | 搜索永远返回空 |
| **LunaTV 返回格式** | 裸 JSON,没有 `code/msg/data` | 反序列化失败 |
| **播放记录格式** | dict 不是 list | 解析崩溃 |
| **豆瓣图片域名** | `img9.doubanio.com` 被拦截 | 图片全白 |
| **LaunchedEffect 位置** | 不要放在 LazyColumn `item{}` 内部 | 滚动时被回收,逻辑失效 |
| **Compose 上下文** | `collectAsState()` 只能在 `@Composable` 中调用 | LazyColumn content lambda 不是 @Composable |
| **ExoPlayer seekTo** | `prepare()` 后不能立即 `seekTo` | seek 被忽略,必须等 `STATE_READY` |
| **源切换** | 不要依赖 LaunchedEffect 监听状态变化 | 有延迟和竞争条件,直接在 onClick 中控制 ExoPlayer |

### 2. 修改代码的核心原则

**直接控制 ExoPlayer,不依赖中间状态**:
```kotlin
// ✅ 正确: 直接在 onClick 中控制
exoPlayer.stop()
exoPlayer.setMediaItem(MediaItem.fromUri(url))
exoPlayer.prepare()
exoPlayer.playWhenReady = true

// ❌ 错误: 通过 ViewModel 状态间接触发
viewModel.switchSource(info)  // → LaunchedEffect 监听 → 加载视频
// 这种方式有延迟和竞争条件!
```

**搜索匹配用精确匹配**:
```kotlin
// ✅ 正确: 标题精确匹配
val normalizedTitle = title.replace(" ", "").lowercase()
val matched = sources.firstOrNull { it.title.replace(" ", "").lowercase() == normalizedTitle }

// ❌ 错误: contains 匹配
val matched = sources.firstOrNull { it.title.contains(title, ignoreCase = true) }
// "晚餐" 会匹配到 "最后的猪肉晚餐"!
```

### 3. 调试方法

**文件日志**:
```kotlin
logDebug("tag", "message")
// 写入: /sdcard/Android/data/com.lechenmusic/files/video_debug.log
```

**ADB 实时日志**:
```bash
adb logcat | grep -E "Video|LeChen|ExoPlayer"
```

### 4. 编译和部署

```bash
./gradlew assembleRelease
# APK: app/build/outputs/apk/release/app-release.apk
# 签名: release.keystore.p12, alias: lechenmusic, pass: lechen123
```

---

## 八、Git 提交记录(2026-07-21)

```
8a1c48a feat: 分类页筛选功能 - 年份/地区/来源三维筛选
037c14c feat: 分类页缓存+分页 - 10分钟缓存+滚动加载更多
d528812 feat: 播放器手势控制 - 左侧亮度/右侧音量/水平快进
38271b0 feat: 首页热门短剧分区 - LunaTV搜索短剧+豆瓣热门综艺
2073dc4 fix: matched nullable 编译错误
795d19e fix: 搜索匹配+分类空白+播放记录保存(#40-42)
a65a37f fix: 编译错误 - resp 未使用 + Composable 上下文问题
d60aacf feat: 源切换位置记忆+源测速+源列表面板(#37-39)
1111a88 fix: 彻底重写源切换 - 直接控制 ExoPlayer
8c93a45 fix: 片源切换+进度条拖动(#35-36)
b7cd58a fix: 片源切换+时间显示+记忆位置(#32-34)
4dc2fc9 fix: 片源切换+分类导航(#29-30)
d300f8e fix: 片源切换播放器停止(#31)
fd6268b fix: 片源/全屏/进度条(#21-24)
3ebd528 fix: 点击推荐电影无反应(#19)
73037c3 fix: 编译错误 - currentSrc 残留引用
3af59c5 fix: 片源/全屏/进度条(#21-24)
576b1c5 fix: VideoInfo 无 episodes 字段
89b44a5 fix: VideoInfo 添加 episodes/episodesTitles
06d3492 fix: 补充 getValue/setValue import
788829f refactor: 搜索结果直接播放(参考Selene-Source)
da1f379 fix: 搜索弹窗+全屏切换+片源切换
0d896e7 fix: 详情页+播放器全面修复
e9c4c80 feat: 影视详情页重构
3fb4483 fix: 添加 HLS 依赖 ← 核心修复
f822e3c debug: 添加文件日志追踪
d77ee98 fix: 编译错误修复
ed12701 docs: 更新影视模块开发总结
```

---

## 九、致谢

感谢 Selene-Source 项目提供的参考实现。我们的影视模块在架构和功能上都参考了它的设计方案。遇到问题时,Selene-Source 的代码是最好的参考资料。
