# LeChenMusic 影视模块开发总结

## 项目概述

**LeChenMusic** 是一个音乐+有声书+影视的 Android APP（Jetpack Compose），服务端基于 Navidrome/Subsonic 协议，影视功能对接 LunaTV。

- **APP端仓库**: https://github.com/yueyoue/LeChenMusic-Player
- **服务端仓库**: https://github.com/yueyoue/LeChenMusic-Server
- **LunaTV 服务**: http://j.tthsdd.top:3000 (admin / shaozi1981)
- **LunaTV 源码**: https://github.com/MoonTechLab/LunaTV
- **参考APP（Selene-Source）**: https://github.com/MoonTechLab/Selene-Source（Flutter，为 LunaTV 配套）
- APP增加的影视模块全部功能直接参考（Selene-Source）的功能和实现方法，我们遇到问题的时候第一时间先看（Selene-Source）的解决方案引用到我们APP。

---

## 一、LunaTV 实际 API（已验证）

⚠️ **LunaTV 返回的是裸 JSON，没有 code/msg/data 包装！**

| 接口 | 方法 | 实际返回格式 | 注意事项 |
|------|------|-------------|---------|
| `/api/login` | POST | `{"ok": true}` | body: `{"username":"admin","password":"***"}` |
| `/api/search` | GET | `{"results": [VideoInfo...]}` | **参数用 `q` 不是 `keyword`** |
| `/api/detail` | GET | `VideoDetail` 裸对象 | 参数: `source`, `id` |
| `/api/playrecords` | GET | `{"source+id": {record...}}` | **是 dict 不是 list** |
| `/api/favorites` | GET | `[VideoInfo...]` | 裸数组 |
| `/api/search/resources` | GET | `[SearchResource...]` | 裸数组 |
| `/api/live/sources` | GET | `[LiveSource...]` | - |
| `/api/live/channels` | GET | `[LiveChannelGroup...]` | - |

**不存在的接口**（返回 404）：
- ❌ `/api/home` 
- ❌ `/api/category`

### 搜索结果 VideoInfo 字段
```json
{
  "id": "65490",
  "title": "痴迷2025",
  "poster": "https://tu.iqiyizyimg.com/upload/vod/...",
  "episodes": ["https://xxx.m3u8", ...],        // ✅ 搜索结果已包含播放链接！
  "episodes_titles": ["1", "2", ...],
  "source": "iqiyizyapi.com",
  "source_name": "🎬-爱奇艺-",
  "class": "",
  "year": "2025",
  "desc": "",
  "type_name": "movie",
  "douban_id": 0,
  "remarks": "",
  "quality_tag": ""
}
```

### 详情 VideoDetail 字段
```json
{
  "id": "80631",
  "title": "",
  "poster": "https://...",
  "episodes": ["https://xxx.m3u8"],
  "episodes_titles": ["1"],
  "source": "iqiyizyapi.com",
  "source_name": "🎬-爱奇艺-",
  "class": "",
  "year": "2025",
  "desc": "",
  "type_name": "",
  "douban_id": 0
}
```

### 播放记录格式（dict，key 是 "source+id"）
```json
{
  "apilsbzy1.com+313262": {
    "title": "xxx",
    "cover": "https://...",
    "year": "2026",
    "source_name": "🔞-老色逼-",
    "index": 1,
    "total_episodes": 1,
    "play_time": 474,
    "total_time": 1465,
    "save_time": 1783320732423
  }
}
```

---

## 二、已修复的问题（可编译通过）

### 1. API 参数名错误
- **问题**: 搜索接口用了 `keyword` 参数，实际应该是 `q`
- **影响**: 搜索永远返回空结果
- **修复**: `VideoApiClient.kt` 中 `@Query("keyword")` → `@Query("q")`

### 2. DoubanApiClient JVM 签名冲突
- **问题**: `private val api` 属性和 `fun getApi()` 方法在 JVM 层面同名
- **修复**: 属性改名为 `_api`

### 3. 豆瓣 API 返回格式变化
- **问题**: 豆瓣 API 不再返回 `cover_url`，改为 `pic.large`/`pic.normal`
- **修复**: 新增 `DoubanPic` 数据模型，`displayCover` 优先读 `pic.normal`

### 4. 豆瓣图片 418 被拦截
- **问题**: `img9.doubanio.com` 拦截非浏览器请求
- **修复**: CDN 替换为 `img3.doubanio.com`（带 Referer 可访问）
- **参考**: Selene-Source 也用同样方案（`official_cdn` 选项）

### 5. getCategory API 不存在
- **问题**: 调用了不存在的 `/api/category` 接口
- **修复**: 改为用搜索接口 `searchCategory(keyword)` 实现分类

### 6. LunaApiResponse 包装不存在
- **问题**: 代码期望 `code/msg/data` 包装，LunaTV 返回裸 JSON
- **修复**: 重写所有数据模型，移除包装层

### 7. 播放记录格式错误
- **问题**: 代码期望 list，实际返回 dict
- **修复**: 从 dict 解析为 list，key 中提取 source+id

---

## 三、未解决的问题（当前代码仍存在）

### ❌ 问题1: 点击热门电影进不去页面 / 闪退

**现象**: 点击豆瓣热门电影后，页面空白或闪退

**根因分析**:
1. 点击豆瓣电影 → 调用 `searchAndPlay(title)` → 搜索 LunaTV
2. 搜索返回结果 → 构造 `VideoDetail` → 设置 `_videoDetail.value`
3. 触发 `needNavigateToPlayer` → 导航到 `VideoPlayerDirect`
4. **VideoPlayerDirect 读取 `detail?.toSources()`**

**可能的失败点**:
- 搜索结果的 `episodes` 字段可能为空（某些源不返回 episodes）
- `VideoDetail` 构造时 `episodes` 字段未正确映射
- `toSources()` 返回空列表 → 显示"未找到播放资源"
- **搜索结果第一条可能不是最佳匹配**（标题不完全匹配）

**已尝试的修复方向**（未完全解决）:
- ✅ 优先选择标题匹配的搜索结果
- ✅ 搜索结果添加 `episodes` 字段到 `VideoInfo`
- ❌ 但可能还有数据映射问题

**建议修复方向**:
1. **加日志**: 在 `searchAndPlay` 方法中打印搜索结果数量、第一条的 title/episodes
2. **验证数据流**: 确认 `VideoInfo.episodes` 是否正确反序列化（Gson 可能有问题）
3. **参考 Selene-Source**: 它的 `PlayerScreen` 自己处理搜索+播放，不经过中间状态

### ❌ 问题2: 最近观看点播放闪退

**现象**: 点击最近观看 → 进详情页 → 点播放 → 闪退

**根因分析**:
1. 最近观看的播放记录有 `source` 和 `id`（从 dict key 解析）
2. 点击 → 导航到 `VideoDetail(source, videoIdRaw)`
3. 调用 `loadDetail(source, id)` → LunaTV `/api/detail`
4. **某些源的 detail 返回空 episodes**（如 `apilsbzy1.com` 返回 0 个 episodes）
5. 点播放 → `toSources()` 返回空 → 闪退

**已验证**: 
```bash
curl "/api/detail?source=apilsbzy1.com&id=313262" → episodes: 0  # 源已失效
curl "/api/detail?source=iqiyizyapi.com&id=80631" → episodes: 1  # 正常
```

**建议修复方向**:
1. **播放前检查 episodes**: 为空时提示"该源暂无播放资源"并尝试其他源
2. **自动换源**: 当前源无资源时，用标题搜索 LunaTV 找其他源
3. **播放记录存储 source_name**: 便于显示和换源

### ❌ 问题3: 首页横向导航菜单

**现状**: 菜单已显示（推荐/电影/剧集/动漫/综艺/直播），但点击分类后内容可能为空

**原因**: 分类页用 `searchCategory("电影")` 搜索，但 LunaTV 搜索可能对"电影"这种泛关键词返回不理想

**建议**: 参考 Selene-Source，分类页用豆瓣 API 获取列表，点击后再搜索 LunaTV 播放

---

## 四、项目架构

### 文件结构
```
app/src/main/java/com/lechenmusic/
├── data/
│   ├── api/
│   │   ├── ApiClient.kt          # 音乐 API（Subsonic/Navidrome）
│   │   ├── SubsonicApi.kt        # 音乐接口定义
│   │   └── VideoApiClient.kt     # 影视 API（LunaTV + 豆瓣）
│   ├── model/
│   │   ├── SubsonicModels.kt     # 音乐数据模型
│   │   └── VideoModels.kt        # 影视数据模型
│   └── repository/
│       └── SettingsRepository.kt # 设置存储（DataStore）
├── ui/
│   ├── MainViewModel.kt          # 音乐主 ViewModel
│   ├── VideoViewModel.kt         # 影视 ViewModel ⭐
│   ├── navi/Screen.kt            # 路由定义
│   ├── screens/
│   │   ├── home/HomeScreen.kt    # 首页（音乐/有声书/影视三模式）
│   │   └── video/
│   │       ├── VideoScreen.kt         # 影视独立页面（未使用）
│   │       ├── VideoSearchScreen.kt   # 搜索
│   │       ├── VideoDetailScreen.kt   # 详情
│   │       ├── VideoPlayerScreen.kt   # 播放器
│   │       ├── VideoCategoryScreen.kt # 分类页
│   │       └── LiveScreen.kt         # 直播
│   └── components/               # 通用组件
├── player/                       # 音乐播放器
├── LeChenApp.kt                  # Application（含 Coil 配置）
└── MainActivity.kt               # 主 Activity（路由注册）⭐
```

### 关键配置
- **compileSdk**: 34, **minSdk**: 26, **targetSdk**: 34
- **Kotlin**: 1.9.x, **Compose BOM**: 需确认版本
- **签名**: `release.keystore.p12`, alias: `lechenmusic`, pass: `lechen123`
- **ProGuard**: 已配置保留 `com.lechenmusic.data.model.**` 和 API 接口

---

## 五、参考项目 Selene-Source 关键实现

Selene-Source 是 Flutter 项目，但架构思路可参考：

### 1. 搜索播放流程（核心！）
```
点击电影 → PlayerScreen(title, year)
  → PlayerScreen 内部调 fetchSourcesData(title)
  → 拿到搜索结果（含 episodes）
  → 直接用第一个结果的 episodes 播放
```
**不经过详情页，搜索结果直接包含播放链接。**

### 2. 图片处理
```dart
// CDN 替换
originalUrl.replaceAll(RegExp(r'img\d+\.doubanio\.com'), 'img3.doubanio.com')

// 请求头
headers = {
  'Referer': 'https://movie.douban.com/',
  'User-Agent': 'Mozilla/5.0 ...',
  'Accept': 'image/avif,image/webp,...'
}
```

### 3. 搜索参数
```dart
'/api/search', queryParameters: {'q': query.trim()}  // 用 q 不是 keyword
```

### 4. 首页结构
```
TopTabSwitcher (首页/播放历史/收藏夹)
  → ContinueWatchingSection (继续观看)
  → HotMoviesSection (热门电影 - 豆瓣)
  → HotTvSection (热门剧集 - 豆瓣)
  → HotShowSection (热门综艺 - 豆瓣)
  → BangumiSection (动漫 - Bangumi)
```

### 5. 关键文件
- `lib/services/api_service.dart` — API 调用
- `lib/services/douban_service.dart` — 豆瓣数据
- `lib/utils/image_url.dart` — 图片 URL 处理
- `lib/screens/player_screen.dart` — 播放器（搜索+播放一体）
- `lib/screens/home_screen.dart` — 首页

---

## 六、给下位程序员的建议

### 优先级排序
1. **🔴 P0**: 修复点击播放闪退（最近观看 + 热门电影）
2. **🔴 P0**: 修复热门电影图片不显示
3. **🟡 P1**: 完善播放器功能（投屏、手势、选集）
4. **🟡 P1**: 响应式布局（手机/平板/车机）
5. **🟢 P2**: 分类页内容完善

### 建议的修复策略

**不要**继续在当前架构上打补丁。建议**重写影视播放流程**，参考 Selene-Source：

1. **新建一个 `VideoSearchPlayScreen`**：搜索 + 播放一体化
   - 接收参数: `title`, `year`（来自豆瓣）或 `source`, `id`（来自播放记录）
   - 内部自己搜索 LunaTV、选择源、播放
   - 不依赖 VideoViewModel 的中间状态

2. **简化数据流**:
   - 豆瓣数据 → 只用于首页展示（封面、标题、评分）
   - 点击后 → 搜索 LunaTV → 直接播放
   - 不要走 详情页 → 播放器 的两步流程

3. **图片加载**: 
   - 用 `img3.doubanio.com` 替换（已有代码）
   - Coil 拦截器加 Referer（已有代码）
   - 如果还不行，考虑用 LunaTV 服务端代理图片

4. **调试方法**:
   - 在关键方法加 `Log.d("Video", "...")` 打日志
   - 用 `adb logcat | grep Video` 看日志
   - 先确认搜索 API 返回数据正常，再处理 UI

### 需要特别注意的坑
1. LunaTV 搜索用 `q` 不是 `keyword`
2. LunaTV 返回裸 JSON，不要期望 `code/msg/data` 包装
3. 播放记录是 dict 不是 list
4. 豆瓣图片域名会被拦截，必须替换 CDN
5. `VideoInfo` 和 `VideoDetail` 是不同的模型，字段不完全一样
6. 某些源的 detail 返回空 episodes，需要做容错

---

## 七、今天的 Git 提交记录

```
89b44a5 fix: VideoInfo 添加 episodes/episodesTitles 字段
06d3492 fix: 补充 getValue/setValue import
788829f refactor: 搜索结果直接播放，跳过详情页（参考Selene-Source）
576b1c5 fix: VideoInfo 无 episodes 字段，改为按标题匹配
55d45f5 fix: 修复详情页空白和播放闪退
cb22fee fix: 豆瓣图片CDN替换为img3.doubanio.com
a529dc8 fix: 修复影视模块核心问题
1015877 fix: 修复编译错误 - background import 和 navController 引用
db96494 fix: 影视模块核心修复
2e1396b fix: 修复4个编译错误 - comingSoon/episodeIndex未定义
92babb4 fix: 补充 Coil import
69732e4 fix: 影视模块全面重构 - 适配LunaTV实际API格式
95d476a fix: 修复 DoubanApiClient JVM 签名冲突
e3fcf61 fix: 移除不存在的 getCategory API 调用
```

**当前状态**: 编译通过，但运行时点击播放仍闪退。问题在数据流和播放器初始化。
